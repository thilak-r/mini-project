services:
  - type: web
    name: glaucoma-detection-app
    env: python
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
